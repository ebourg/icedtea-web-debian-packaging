#!/bin/sh

set -e

basedir=/@basedir@
tools='javaws'

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    for i in $tools; do
        update-alternatives --remove $i $basedir/jre/bin/$i
    done
fi

#DEBHELPER#
